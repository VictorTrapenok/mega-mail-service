---
# Требуемое состояние воркеров: running или stopped.
# Значение задаётся вызывающим плейбуком; безопасный запасной вариант —
# рабочее состояние, чтобы забытый вызов не оставил стенд без доставки.
bench_workers_state: running
