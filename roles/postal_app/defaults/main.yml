---

# Signing key size. The Postal bootstrap generates 1024 bits; we keep
# that value so the baseline build matches the upstream default.
# The size affects the cost of signing and is therefore pinned explicitly.
postal_signing_key_bits: 1024

postal_web_max_threads: 5
