---
# Идентификатор прогона задаётся плейбуком; здесь только запасное значение.
#
# Команда запуска генератора живёт в group_vars/all/50-bench.yml: её используют
# несколько ролей, а default роли виден только пока эта роль выполняется.
bench_run_id: "manual"
