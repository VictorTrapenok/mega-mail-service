---
# The run identifier is set by the playbook; this is only a fallback value.
#
# The generator launch command lives in group_vars/all/50-bench.yml: it is used by
# several roles, and a role default is only visible while that role is executing.
bench_run_id: "manual"
