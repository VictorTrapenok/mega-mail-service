---
# Насколько далеко назад смотреть в логах воркера при поиске молчаливых
# удалений. Grep по всему логу накапливался бы через серию прогонов,
# и первое же удаление помечало бы все последующие прогоны.
bench_run_seconds: "{{ (bench_warmup_s | int) + (bench_duration_s | int) + (bench_drain_timeout_s | int) }}"
