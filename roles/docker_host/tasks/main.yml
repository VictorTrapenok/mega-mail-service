---
- name: Remove the distribution Docker packages
  ansible.builtin.apt:
    name:
      - docker.io
      - docker-compose
      - docker-doc
      - podman-docker
      - runc
    state: absent

- name: Add the Docker repository
  ansible.builtin.deb822_repository:
    name: docker
    types: [deb]
    uris: "{{ docker_apt_repo_url }}"
    suites: ["{{ ansible_facts.distribution_release }}"]
    components: ["{{ docker_apt_channel }}"]
    signed_by: "{{ docker_apt_gpg_key }}"
    state: present
    enabled: true
  register: docker_repo

- name: Update the apt cache
  ansible.builtin.apt:
    update_cache: true
  when: docker_repo.changed

- name: Install Docker
  ansible.builtin.apt:
    name: "{{ docker_packages }}"
    state: present
  when: not docker_pin_versions

- name: Install Docker at the pinned versions
  ansible.builtin.apt:
    name: "{{ docker_packages | zip(docker_packages | map('extract', docker_pinned_versions)) | map('join', '=') | list }}"
    state: present
    allow_downgrade: true
  when: docker_pin_versions

- name: Freeze the Docker package versions
  ansible.builtin.dpkg_selections:
    name: "{{ item }}"
    selection: "{{ 'hold' if docker_pin_versions else 'install' }}"
  loop: "{{ docker_packages }}"

- name: Configure the Docker daemon
  ansible.builtin.copy:
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: "0644"
    content: |
      {
        "log-driver": "{{ docker_log_driver }}",
        "log-opts": {
          "max-size": "{{ docker_log_max_size }}",
          "max-file": "{{ docker_log_max_file }}"
        }
      }
  notify: Restart Docker

- name: Enable Docker
  ansible.builtin.systemd:
    name: docker
    state: started
    enabled: true

- name: Apply the deferred Docker restart before any further tasks
  ansible.builtin.meta: flush_handlers

- name: Capture the Docker and Compose versions for the report
  ansible.builtin.shell:
    cmd: |
      set -o pipefail
      printf '%s\t%s\n' \
        "$(docker version --format '{{ '{{' }}.Server.Version{{ '}}' }}')" \
        "$(docker compose version --short)"
    executable: /bin/bash
  changed_when: false
  register: docker_versions

- name: Store the Docker versions in host facts
  ansible.builtin.set_fact:
    stand_docker_version: "{{ docker_versions.stdout.split('\t')[0] }}"
    stand_compose_version: "{{ docker_versions.stdout.split('\t')[1] }}"
    cacheable: true
